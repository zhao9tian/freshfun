<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quxin.freshfun.dao.AddressUtilMapper">
  <!--获取省份-->
  <select id="selectProvince" resultType="com.quxin.freshfun.model.pojo.address.AddressUtilPOJO">
    select areano as areaNo,areaname as areaName,parentno as parentNo,arealevel as areaLevel from prov_city_area where parentno = 0
  </select>

  <select id="selectCityArea" resultType="com.quxin.freshfun.model.pojo.address.AddressUtilPOJO" parameterType="java.lang.Integer">
    select areano as areaNo,areaname as areaName,parentno as parentNo,arealevel as areaLevel from prov_city_area where parentno = #{parentNo}
  </select>

</mapper>